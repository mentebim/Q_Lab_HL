# Promotion Record

Every promoted champion should carry a promotion record that explains:

- stage
- selector policy
- source result artifact
- candidate identity
- timestamp and promotion reason

That is how the handoff to `Execution` stays traceable.
