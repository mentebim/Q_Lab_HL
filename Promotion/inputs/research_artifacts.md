# Research Artifacts

`Promotion` consumes the artifacts emitted by `AgentHL`.

Required inputs:
- leaderboard rows
- result artifacts
- acceptance data
- promotion-eligibility metadata

Promotion must not invent missing research state.
