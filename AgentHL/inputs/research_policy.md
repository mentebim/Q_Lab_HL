# Research Policy

The `AgentHL` branch is allowed to search only inside a bounded research contract.

Mutable surface:
- candidate specs
- approved strategy-family parameters
- bounded research metadata

Fixed surface:
- judge semantics
- promotion behavior
- execution behavior

Selection contract:
- pass express filter first
- then pass the full judge
- only then become eligible for paper or live promotion
