# Leaderboard

`AgentHL` appends a compact leaderboard row for each experiment.

The leaderboard is the fast summary surface for:
- recent research history
- current frontier
- promotion candidate screening

`Promotion` reads this downstream but does not create it.
