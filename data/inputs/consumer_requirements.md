# Consumer Requirements

The `data` branch must satisfy both downstream consumers:

- `AgentHL` needs research-safe historical data
- `Execution` needs fresh runtime-safe data

The cache contract must support both without ambiguity.
