# Branch Surface

The intended `data` production branch should contain:

- ingestion code
- cache write logic
- freshness validation
- schema metadata
- data-focused tests

Today the current `data` branch pointer still reflects the older integrated snapshot, so this folder describes the target role more than the current trim state.
