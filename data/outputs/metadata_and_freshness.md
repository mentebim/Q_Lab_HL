# Metadata And Freshness

The `data` branch should also publish:

- schema metadata
- coverage metadata
- freshness status
- validation status

This allows downstream branches to fail safely on stale or malformed data.
