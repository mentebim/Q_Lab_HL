# Trusted Cache

The primary output of `data` is one trusted market cache.

That cache is the input to both:
- `AgentHL`
- `Execution`

It should be the only market-data truth surface used by the rest of the system.
