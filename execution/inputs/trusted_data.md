# Trusted Data

`Execution` consumes the same trusted cache published by `data`.

It uses that cache to:
- refit the pinned model
- compute target weights
- align runtime with the same market view used in research
